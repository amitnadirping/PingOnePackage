import XCTest
@testable import PingOneSDKPackage

final class PingOneSDKPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PingOneSDKPackage().text, "Hello, World!")
    }
}
